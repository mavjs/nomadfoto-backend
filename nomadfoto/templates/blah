<html metal:use-macro="load: base.pt">
    <style type="text/css">
        body {
            padding-bottom: 40px;
        }

      .form-signin {
        max-width: 300px;
        padding: 19px 29px 29px;
        margin: 0 auto 20px;
        background-color: #f5f5f5;
        border: 1px solid #e5e5e5;
        -webkit-border-radius: 5px;
           -moz-border-radius: 5px;
                border-radius: 5px;
        -webkit-box-shadow: 0 1px 2px rgba(0,0,0,.05);
           -moz-box-shadow: 0 1px 2px rgba(0,0,0,.05);
                box-shadow: 0 1px 2px rgba(0,0,0,.05);
      }
      
      .form-signin .form-signin-heading,
      .form-signin .checkbox {
        margin-bottom: 10px;
      }
      .form-signin input[type="text"],
      .form-signin input[type="password"] {
        font-size: 16px;
        height: auto;
        margin-bottom: 15px;
        padding: 7px 9px;
      }
    </style>
    <!-- <div class="container" metal-fill-slot="main-content">
        <form class="form-signin" action="${url}" method="post">
                    <h2 class="form-signin-heading">Please sign in</h2>
                    <input type="hidden" name="came_from" value="${came_from}" />
                    <input type="text" class="input-block-level" name="username=" value="${username}" placeholder="Email address"/>
                    <input type="password" class="input-block-level" name="password" value="${password}" placeholder="Password" />
             <label class="checkbox">
                      <input type="checkbox" value="remember-me"> Remember me
                  </label>
                  <button class="btn btn-large btn-primary" type="submit" name="form.submitted">Log in</button>
        </form>
    </div> -->
</html>
